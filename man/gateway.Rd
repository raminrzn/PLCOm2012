% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prism_server.R
\name{gateway}
\alias{gateway}
\title{ModelsCloud / PRISM gateway (server entry point)}
\usage{
gateway(...)
}
\arguments{
\item{...}{Request fields supplied by the platform. Recognised control
fields are \code{func} (the function to run), \code{api_key}, and \code{session_id}; all
other fields (typically \code{model_input}) are passed to the selected function.}
}
\value{
A JSON string produced by \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}} containing the
selected function's result.
}
\description{
Generic dispatcher invoked by the ModelsCloud platform. It reads \code{func} from
the incoming request, strips the platform-only fields (\code{func}, \code{api_key},
\code{session_id}), and calls the named package function with the remaining
arguments — e.g. \code{gateway(func = "model_run", model_input = ...)} runs
\code{\link[=model_run]{model_run()}}. If \code{func} is omitted it defaults to \code{"model_run"}. The result
is serialised to JSON, matching the established gateway contract.
}
\seealso{
\code{\link[=model_run]{model_run()}}, \code{\link[=get_default_input]{get_default_input()}}, \code{\link[=get_sample_input]{get_sample_input()}}
}
